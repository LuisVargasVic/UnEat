<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fernandalopezcardenas.uneatfinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainClientActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".PlateActivity" />
        <activity android:name=".RestaurantActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".CarActivity" />
        <activity android:name=".ShowCarActivity" />
        <activity android:name=".PayActivity" />
        <activity android:name=".DataActivity" />
        <activity android:name=".MainRestaurantActivity"></activity>
    </application>

</manifest>